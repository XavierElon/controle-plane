services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: starling
      POSTGRES_PASSWORD: starling
      POSTGRES_DB: starling
    ports:
      - '5437:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
